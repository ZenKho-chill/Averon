name: ping
version: 1.0.0
runtime:
  language: typescript
  engine: node
  version: ">=18"
  transport: in-process
entry: src/index.ts
commands:
  - name: ping
    description:
      vi: "Lệnh ping"
      en: "Ping command"
    handler: commands/ping.ts
    type: chat_input   # chat_input | user | message (context menu)
    scope: [global]    # global | guild | user (mặc định global)