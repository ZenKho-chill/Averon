# ===== Dependencies =====
node_modules/
package-lock.json

# ===== Build output =====
dist/
build/
*.tsbuildinfo

# ===== Logs & crash reports =====
logs/
crash-reports/
*.log

# ===== Secrets (KHÔNG commit token thật / never commit real tokens) =====
# Token đặt trực tiếp trong config/dev.yml + config/prod.yml (CLAUDE.md §6.3).
# Repo này PUBLIC — nếu lỡ commit token thật, hãy reset token ngay trên Discord Developer Portal.
# EN: Tokens live in config/dev.yml + config/prod.yml. This repo is PUBLIC — if a real
# token is ever committed, revoke it immediately in the Discord Developer Portal.
# Không còn dùng file .env / .env.example.

# ===== Test coverage =====
coverage/

# ===== Native build artifacts (modules C/C++/Rust) =====
*.o
*.obj
*.so
*.dll
*.dylib

# ===== OS =====
.DS_Store
Thumbs.db

# ===== Editor / IDE =====
.vscode/
.idea/
*.swp
